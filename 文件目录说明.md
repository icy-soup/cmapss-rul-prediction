# 项目文件目录说明

## 根目录

| 文件      | 用途                                                                            |
| --------- | ------------------------------------------------------------------------------- |
| `main.py` | 统一实验入口。支持 `--model {base,person1,person2}` 和 `--subset {FD001~FD004}` |

## docs/ — 文档

| 文件                                 | 用途                                     |
| ------------------------------------ | ---------------------------------------- |
| `CMAPSS_RUL_技术报告.html`           | 正式版 HTML 报告，专业格式，不含表情符号 |
| `RUL_iteration_roadmap.docx`         | 五人接力迭代架构路线图                   |
| `作业要求.md`                        | 大作业要求说明                           |
| `基于深度学习的剩余使用寿命预测.pdf` | 背景文献                                 |

## src/ — 源代码

### step1_preprocessing/ — 数据加载与预处理

| 文件        | 用途                                                      |
| ----------- | --------------------------------------------------------- |
| `loader.py` | 读取 CMAPSS 原始数据，全局/按工况归一化，滑动窗口生成样本 |

### step2_features/ — 特征提取

| 文件          | 用途                                                        |
| ------------- | ----------------------------------------------------------- |
| `patching.py` | PatchEmbedding（线性投影）和 ConvPatchEmbedding（卷积投影） |

### step3_models/ — 模型定义

| 文件               | 用途                                                                               |
| ------------------ | ---------------------------------------------------------------------------------- |
| `base_model.py`    | Base: Patch-iTransformer-RUL（线性 Patch + iTransformer + MLP）                    |
| `person1_model.py` | Person 1: ConvPatch-iTransformer-RUL（卷积 Patch + iTransformer + MLP）            |
| `person2_model.py` | Person 2: MSPatch-iTransformer-RUL（三分支 ConvPatch + 融合 + iTransformer + MLP） |
| `trainer.py`       | 训练器：MSE Loss + AdamW + WarmupCosine + Early Stopping                           |

### step4_visualization/ — 可视化

| 文件               | 用途                                                                           |
| ------------------ | ------------------------------------------------------------------------------ |
| `plot_results.py`  | 生成训练曲线、散点图、引擎预测、指标对比等所有图片，支持全模型和精简版两套输出 |
| `plot_ablation.py` | 生成消融实验对比图（分支数量、融合方式、FD004 验证、总结）                     |

### step5_pipeline/ — 实验流水线

| 文件                | 用途                                                                                    |
| ------------------- | --------------------------------------------------------------------------------------- |
| `ablation.py`       | 自动运行 FD001 消融实验（7 种分支配置 + 3 种融合方式），保存结果                        |
| `ablation_fd004.py` | 运行 FD004 消融实验（small+medium, single-small, single-medium）                        |
| `export_excel.py`   | 将所有实验结果导出为 xlsx，含 Summary/Ablation FD001/Ablation FD004/Training 四个工作表 |

### system/ — 系统配置与工具

| 文件         | 用途                                                     |
| ------------ | -------------------------------------------------------- |
| `config.py`  | BaseConfig 数据类：所有超参数默认值 + SUBSET_CONFIG 预设 |
| `metrics.py` | 评估指标实现：RMSE, PHM08 Score, R²                      |

## results/ — 实验结果

每个子目录内均有 `说明.txt` 详细描述内容。

### results/ 根目录

| 文件                  | 用途                                                            |
| --------------------- | --------------------------------------------------------------- |
| `summary.csv`         | 所有 12 组实验（3 模型 × 4 子集）的 RMSE, R², Score, 参数量汇总 |
| `CMAPSS_Results.xlsx` | Excel 版汇总，含消融实验和训练历史                              |

### results/main_experiments/ — 主实验数据（12 组）

每组实验包含以下 5 个文件（以 `PatchiTransformerRUL_FD001` 为例）：

| 文件类型         | 示例                                      | 用途                                        |
| ---------------- | ----------------------------------------- | ------------------------------------------- |
| `*_metrics.json` | `PatchiTransformerRUL_FD001_metrics.json` | 评估指标：RMSE, R², Score（JSON 格式）      |
| `*_config.json`  | `PatchiTransformerRUL_FD001_config.json`  | 实验配置参数                                |
| `*_history.npz`  | `PatchiTransformerRUL_FD001_history.npz`  | 训练历史：train_loss, val_loss, val_rmse 等 |
| `*_y_pred.npy`   | `PatchiTransformerRUL_FD001_y_pred.npy`   | 测试集预测值（NumPy 数组）                  |
| `*_y_test.npy`   | `PatchiTransformerRUL_FD001_y_test.npy`   | 测试集真实 RUL 值（NumPy 数组）             |

主实验清单：12 组 = 3 模型（Base/P1/P2）× 4 子集（FD001~FD004）

模型命名对照：
- `PatchiTransformerRUL`     = Base（Patch-iTransformer-RUL）
- `ConvPatchiTransformerRUL`  = Person 1（ConvPatch-iTransformer-RUL）
- `MSPatchiTransformerRUL`    = Person 2（MSPatch-iTransformer-RUL，本项目核心）

### results/ablation/ — 消融实验数据

| 文件/系列 | 内容 |
|-----------|------|
| `ablation_summary.csv` | 所有消融实验指标汇总 |
| `ablation_A0-*` ~ `ablation_A6-*` | FD001 分支数量消融（A0:单小 ~ A6:三分支） |
| `ablation_B0-*` ~ `ablation_B2-*` | FD001 融合方式消融（Gated/Concat/Weighted Sum） |
| `ablation_FD004_*` | FD004 消融（single-small/medium, small+medium） |

### results/figures/ — 图片

每个文件夹内均有 `说明.txt` 详细描述内容。

#### 根目录：精简版对比图（Base + Person2）

| 文件 | 内容 |
|------|------|
| `fig_training.png` | 训练曲线对比（Base+P2，4 子集） |
| `fig_scatter.png` | 预测散点图对比（Base+P2） |
| `fig_per_engine.png` | 引擎级预测对比（Base+P2） |
| `fig_comparison.png` | 指标对比柱状图（Base+P2） |
| `fig_ablation_branches.png` | 消融：分支数量对比（7 种配置） |
| `fig_ablation_fusion.png` | 消融：融合方式对比（3 种） |
| `fig_ablation_fd004.png` | 消融：FD004 验证 |
| `fig_ablation_summary.png` | 消融总结 |

#### full/ — 全模型大版（Base+P1+P2，12 子图合并）

适合整体概览，一次性对比全部 3 个模型 × 4 个子集。

| 文件 | 内容 |
|------|------|
| `fig_training.png` | 训练曲线（3×4=12 子图） |
| `fig_scatter.png` | 散点图（12 子图） |
| `fig_per_engine.png` | 引擎预测（12 子图） |
| `fig_comparison.png` | 指标对比柱状图 |

#### 按子集/ — 按 FD001~FD004 子集分类

**全模型/**（Base+P1+P2 三模型并排，适合整体对比）

| 子目录 | 每子集文件 | 内容 |
|--------|-----------|------|
| `训练曲线/` | FD001.png~FD004.png | 三模型训练曲线对比，每子集一张 |
| `散点图/` | FD001.png~FD004.png | 三模型预测散点图对比 |
| `引擎预测/` | FD001.png~FD004.png | 三模型引擎级预测对比 |

**精简(Base+P2)/**（双模型并排，适合论文展示）

| 子目录 | 每子集文件 | 内容 |
|--------|-----------|------|
| `训练曲线/` | FD001.png~FD004.png | Base+P2 训练曲线对比 |
| `散点图/` | FD001.png~FD004.png | Base+P2 散点图对比 |
| `引擎预测/` | FD001.png~FD004.png | Base+P2 引擎预测对比 |

#### 消融实验/ — 消融研究可视化

| 文件 | 内容 | 结论 |
|------|------|------|
| `分支数量消融.png` | 7 种分支配置 RMSE/R² 对比 | small+medium 双分支最优 |
| `融合方式消融.png` | 3 种融合方式对比 | Concat 融合最优 |
| `FD004验证.png` | FD004 单/双分支对比 | 验证多分支在复杂工况表现 |
| `消融总结.png` | 消融实验综合总结 | — |

## archive/ — CMAPSS 原始数据

CMAPSS 的四个子集数据文件：

- `train_FD001.txt` ~ `train_FD004.txt` — 训练集
- `test_FD001.txt` ~ `test_FD004.txt` — 测试集
- `RUL_FD001.txt` ~ `RUL_FD004.txt` — 测试集真实 RUL

## literature/ — 参考文献 PDF

8 篇参考文献 PDF 文件。

## .claude/ — Claude 配置

| 文件        | 用途                                     |
| ----------- | ---------------------------------------- |
| `CLAUDE.md` | 项目级记忆：当前状态、实验记录、待做清单 |

---

_最后更新：2026-06-04_
